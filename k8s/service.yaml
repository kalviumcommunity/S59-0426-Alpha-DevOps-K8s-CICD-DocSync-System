apiVersion: v1
kind: Service
metadata:
  name: docsync
  labels:
    app: docsync
spec:
  type: ClusterIP
  selector:
    app: docsync
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
